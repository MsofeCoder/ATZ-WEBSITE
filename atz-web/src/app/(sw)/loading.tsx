import AtzLoader from "@/components/AtzLoader";

/** Instant loading UI for client-side navigations within the Swahili tree. */
export default function Loading() {
  return <AtzLoader lang="sw" />;
}
